:includesource: ../../../examples/basics

= Plugin Configuration

How to configure plugins for the life cycle


== General Configuration

Let us start with a minimal `pom` file.

[source,xml]
----
include::{includesource}/simple/pom.xml[tags=first]
----
<1> The groupId
<2> The artifactId
<3> The version


:keywords: comma-separated values, stored, in an HTML, meta, tag

There are several options to configure a plugin.

 *
 *

== The build life cycle
